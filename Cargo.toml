[workspace]

members = [
    "theseus",
    "theseus_cli",
    "theseus_playground",
    "theseus_gui/src-tauri",
    "theseus_macros"
]

# Optimize for speed and reduce size on release builds
[profile.release]
codegen-units = 1
lto = true
strip = true