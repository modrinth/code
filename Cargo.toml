[workspace]

members = [
    "theseus",
    "theseus_cli",
    "theseus_gui/src-tauri"
]

# Optimize for speed and reduce size on release builds
[profile.release]
codegen-units = 1
lto = true
strip = true