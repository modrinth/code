.stat {
	display: flex;
	align-items: baseline;
	grid-gap: 0.5rem;

	&--light {
		color: var(--color-text-lightest);
	}

	.icon {
		align-self: center;
	}

	strong {
		font-size: var(--font-size-lg);
	}
}

.stat-group {
	display: flex;
	grid-gap: 0.5rem;
	flex-wrap: wrap;
}
