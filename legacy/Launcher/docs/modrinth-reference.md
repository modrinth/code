# Modrinth reference (local)

Clone path (sibling of this repo):

`Z:\!Others\!Dev\modrinth-code`

Remote: https://github.com/modrinth/code  

Sparse checkout of launcher-relevant packages:

- `apps/app` — Tauri shell  
- `apps/app-frontend` — Vue UI  
- `packages/app-lib` — Theseus (instances / Java / launch)  
- `packages/ui`, `packages/assets`

**Phase 1:** port behavior and UI structure into Owyx (`apps/launcher`) as a working Modrinth-like reference.  
**Phase 2:** Owyx brand, colors, logos, private servers, own login, possible layout redesign.

Do not commit the Modrinth clone into this git repo.
