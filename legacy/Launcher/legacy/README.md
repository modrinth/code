# Legacy C# prototype (reference only)

This folder is **not built** and is **not** part of Owyx Launcher.

It keeps selected source from the old WinForms prototype so we can reuse *ideas*
(server ping, Java discovery, launch args, logging) while rewriting in Rust + Tauri.

- Do not add secrets here.
- Do not restore SFTP passwords or production hosts into committed files.
- See root [PLAN.md](../../PLAN.md).
