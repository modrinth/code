{
  "name": "@modrinth/app-lib",
  "scripts": {
    "build": "cargo build",
    "lint": "cargo clippy",
    "fix": "cargo fmt && cargo clippy --fix",
    "test": "cargo test"
  },
  "dependencies": {
    "@modrinth/app-macros": "workspace:*"
  }
}
